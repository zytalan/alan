"""Data providers. Every source of facts (AI, curated files, external datasets)
implements the same interface and emits the same normalized nodes/edges, so new
sources can be added without touching the rest of the system.
"""
